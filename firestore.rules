rules_version = '2';

// 全站資料只透過伺服器端 Admin SDK 讀寫（Admin SDK 會繞過這些規則）。
// 前端一律禁止直接存取 Firestore，杜絕未授權的讀寫。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
